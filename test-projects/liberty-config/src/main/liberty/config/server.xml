<server description="Liberty test server">

    <featureManager>
        <!-- ERROR: misspelled feature name (correct: servlet-6.0) -->
        <feature>servlet-6.X</feature>
        <!-- OK: valid feature -->
        <feature>jsonp-2.0</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080"
                  httpsPort="9443" />

</server>
